services:
  finguru:
    build: .
    ports:
      - "8080:80"

  storybook:
    image: node:20-alpine
    working_dir: /app
    command: sh -c "npm install && npx storybook dev --host 0.0.0.0 --port 6006"
    ports:
      - "6006:6006"
    volumes:
      - .:/app
      - /app/node_modules
