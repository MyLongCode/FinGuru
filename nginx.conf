server {
    listen 80;
    server_name _;

    location /guru {
        alias /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /guru/index.html;
    }

    location / {
        return 404;
    }
}
