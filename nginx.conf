server {
    listen 80;
    server_name _;
    absolute_redirect off;

    root /usr/share/nginx/html;

    location /guru/ {
        try_files $uri $uri/ /guru/index.html;
    }

    location = /guru {
        return 301 /guru/;
    }

    location / {
        return 404;
    }
}
